/** support for deferred image generation.
 * 
 * Chart definitions are captured as XML documents and stored
 * instead of generating the image immediately.
 * 
 * The chart image is actually calculated when the image is first requested
 * 
 */
package uk.ac.ed.epcc.safe.accounting.reports.deferred;